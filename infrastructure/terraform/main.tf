locals {
  environment = var.environment
}

# TODO: Add Terraform resources for Cloud SQL, Secret Manager bindings, VPC connectors, etc.
# This scaffold exists to wire Terraform into CI/CD with remote GCS backend.
